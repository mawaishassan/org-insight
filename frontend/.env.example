# Point the browser at FastAPI directly so long requests (report preview, imports)
# are not proxied by Next.js dev server (avoids ECONNRESET / socket hang up).
NEXT_PUBLIC_API_URL=http://localhost:8080
